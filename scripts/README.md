# Scripts Directory

This directory contains Python scripts for data processing and utilities.

## Scripts

- `ajouter_label_binaire.py` - Adds binary labels (Cancer/Healthy) to the dataset

## Usage

Run scripts from the project root directory:

```bash
python scripts/ajouter_label_binaire.py
```

## Adding New Scripts

When adding new scripts:
- Use clear, descriptive names
- Add docstrings and comments
- Handle errors gracefully
- Use relative paths for file access
